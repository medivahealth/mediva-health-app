# Backend URL for Vite dev proxy (no /api suffix — paths are /api/... on the server)
# Local backend:
VITE_API_PROXY_TARGET=http://127.0.0.1:3000
# Same machine, explicit LAN IP (if 127.0.0.1 fails):
# VITE_API_PROXY_TARGET=http://192.168.1.5:3000
